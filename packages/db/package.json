{
  "name": "db",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prisma:studio": "prisma studio",
    "prisma:dev:gen": "NODE_ENV=development && prisma generate",
    "prisma:dev:migrate": "NODE_ENV=development && prisma migrate",
    "prisma:dev:push": "NODE_ENV=development && prisma db push",
    "prisma:prod:gen": "NODE_ENV=production && prisma generate",
    "prisma:prod:migrate": "NODE_ENV=production && prisma migrate",
    "prisma:prod:push": "NODE_ENV=production && prisma db push"
  },
  "author": "Yoshiki Uchida",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.0.1",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "prisma": "^6.0.1"
  }
}
