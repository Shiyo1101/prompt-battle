{
  "name": "@package/db",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "init": "prisma generate && prisma db push",
    "studio": "prisma studio",
    "dev:gen": "NODE_ENV=development && prisma generate",
    "dev:migrate": "NODE_ENV=development && prisma migrate dev",
    "dev:push": "NODE_ENV=development && prisma db push",
    "prod:gen": "NODE_ENV=production && prisma generate",
    "prod:migrate": "NODE_ENV=production && prisma migrate",
    "prod:push": "NODE_ENV=production && prisma db push"
  },
  "author": "Yoshiki Uchida",
  "license": "ISC",
  "prisma": {
    "schema": "prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.0.1",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "prisma": "^6.0.1"
  }
}